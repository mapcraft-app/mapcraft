stages:
  - build
#  - release

build:
  stage: build
  image: electronuserland/builder:wine
  script:
    - echo "Building stage"
    - echo "${CI_JOB_ID}" > CI_JOB_ID.txt
    - apt install gconf2 gconf-service libnotify4 libappindicator1 libxtst6 libnss3 
    - yarn
    - yarn dist
  artifacts:
    paths:
      - ./out/
    expire_in: 30 days

#release:
#  stage: release
#  image
    
#deploy-job:
#  stage: deploy 
#  script:
#    - echo "Deploying application..."
#    - echo "Application successfully deployed."
